/**
 * Momentum — Custom Supabase Configuration Template
 * 
 * To connect Momentum to your personal Supabase cloud database:
 * 1. Copy or rename this file to "config.js" in the same directory.
 * 2. Replace the placeholder values below with your Project URL and anon/public key.
 *    (Found in your Supabase Dashboard: Project Settings > API)
 * 3. "config.js" is listed in .gitignore so your keys will not be committed to GitHub.
 * 
 * Note: If config.js is not present or keys are empty, Momentum automatically operates
 * in offline Guest Mode with full localStorage persistence.
 */

window.MOMENTUM_SUPABASE_URL = "https://your-project-ref.supabase.co";
window.MOMENTUM_SUPABASE_KEY = "your-anon-key-here";
